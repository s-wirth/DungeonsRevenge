{
  "name": "wirthlike",
  "version": "0.0.1",
  "description": "Sophie's roguelike",
  "main": "index.js",
  "dependencies": {
    "babel-core": "^5.8.29",
    "babel-eslint": "^4.1.3",
    "babel-loader": "^5.3.2",
    "eslint": "^1.7.3",
    "eslint-config-airbnb": "^0.1.0",
    "firebase-tools": "^2.0.2",
    "webpack": "^1.12.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/wmadden/wirthlike.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wmadden/wirthlike/issues"
  },
  "homepage": "https://github.com/wmadden/wirthlike#readme",
  "config": {
    "src": "src",
    "cssOutputDir": "out/css",
    "dist": "dist",
    "cssInputDir": "src/css"
  },
  "scripts": {
    "build-test": "NODE_ENV=test bin/exec webpack --config .webpack-config",
    "build-production": "NODE_ENV=production bin/exec webpack --config .webpack-config",
    "build": "bin/exec webpack --config .webpack-config",
    "clean": "rm -rf ${npm_package_config_dist}",
    "eslint": "bin/exec eslint test/ src/",
    "watch": "bin/exec webpack --config .webpack-config --watch",
    "serve": "bin/exec webpack-dev-server --config .webpack-config"
  }
}
